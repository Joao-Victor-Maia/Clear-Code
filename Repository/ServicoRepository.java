public class ServicoRepository extends RepositoryGenerico<Servico> { 
    // Repositório em memória para serviços.
    // Ele herda salvar(), listar(), buscarPorId() e remover() de RepositoryGenerico.
}
