public class VeiculoRepository extends RepositoryGenerico<Veiculo> { 
    // Repositório em memória para veículos.
    // Ele herda salvar(), listar(), buscarPorId() e remover() de RepositoryGenerico.
}
