public class FornecedorRepository extends RepositoryGenerico<Fornecedor> { 
    // Repositório em memória para fornecedores.
    // Ele herda salvar(), listar(), buscarPorId() e remover() de RepositoryGenerico.
}
